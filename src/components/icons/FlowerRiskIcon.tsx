import { MaterialCommunityIcons } from "@expo/vector-icons";
import { IIcon } from "~/interfaces/IIcon";

export const FlowerRiskIcon = ({ size, color }: IIcon) => (
  <MaterialCommunityIcons name="flower-pollen" size={size} color={color} />
);
