import { Ionicons } from "@expo/vector-icons";
import { IIcon } from "~/interfaces/IIcon";

export const FlowerRiskIcon = ({ size, color }: IIcon) => (
  <Ionicons name="flower-sharp" size={size} color={color} />
);
